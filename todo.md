# Todo

- [x] Authenticate kiosks. Each kiosk should authenticate itself on boot to be able to access APIs.
- [x] There is no token validation in the socket auth handler. It's just blindly trusted.
- [x] Respect `includeIndividuals` and `includeGroups` when importing data using the Scoutnet data source.
- [ ] Admin view to see group checking process. Might be separate system.
- [ ] Map to show on info screens showing checkin process. Map app maybe?
- [x] Add step to block progression. For example if not leader.
- [ ] The GitHub Expressions package now works with ESM. Update and remove clone.
- [ ] Investigate whether removing `external: pluginExternals` from `vite.config.ts` breaks HMR for plugin code in dev. Removed it to fix bare specifier error in prod (`@scouterna/scoutin-plugin-base/frontend` not remapped). Claim is that `build.rollupOptions.external` is prod-only and doesn't affect the dev server.
- [ ] `loadAllDataSourcesIntoDatabase` should run on a schedule or something like that.
- [ ] Add auth to the admin interface and routes.
- [ ] Two steps with the same `uses` value in a config (e.g. two `base:message` steps) will cause the second to be silently skipped — `findNextStepDefinition` matches on `stepId` and finds the first completion record. Either enforce uniqueness of `uses` per config or track step index in `CheckinSessionStepData`.
- [ ] Kiosk: When network goes out, the websocket connection is lost and not reconnected.
- [ ] `identify.ts`'s `searchCandidates` silently drops a matched participant whose `dataSource` is no longer present in `dataSourceConfig.yml` (e.g. stale rows left over from a data source since removed/commented out) instead of surfacing it anywhere. Fixed the crash this used to cause (it previously threw), but we should log or warn when this happens so it doesn't go unnoticed.
- [ ] Enrichers have no way to declare which data source(s) they're valid for/depend on — nothing stops e.g. a Scoutnet-shaped enricher (`scoutnet:safeFromHarm`) being wired to a Google Sheets source via `enrichWith`. Consider adding a declared dependency.
- [ ] Add a non-blocking step/report that surfaces compliance status (Safe from Harm / criminal record extract) for every subject in a session, not just the staff self-check-in gate — relevant for the `groups`/leader flow where actor != subjects.
- [ ] `scoutnet:safeFromHarm`/`scoutnet:criminalRecordExtract` only check current-moment validity. Events can span a week+; add a configurable date until which they must remain valid so a mid-event expiry doesn't slip through.
- [ ] Investigate whether it's safe to change `stepConfig.yml`/`dataSourceConfig.yml` while an event is live (sessions in-progress) — `step.service.ts` caches loaded configs per `configFile` (`configCache`), and `CheckinSession` stores only a `configFile` string, not a content snapshot.
- [ ] Make the admin `/admin/checkin` "Extra info" panel (`StaffInfoPanel.tsx`) update immediately (e.g. push over the session websocket) instead of polling.
- [ ] Make the back button in the admin check-in UI (kiosk flow embedded in `/admin/checkin`) clearer — move it to the left and add an icon.
- [ ] Rework the copy on the compliance gate's blocked screen (`ComplianceGateBlockedScreen.tsx`) for Safe from Harm / criminal record extract failures.
- [ ] Allow the user to skip `scoutnet:complianceGate` (currently has no override — a failing person is always routed to `session:abort` with no way to proceed).
- [ ] `writeBackScoutnetCheckins` reloads the entire checked-in population every sync cycle: the SQL prefilter matches every row with `confirmedCheckedInAt != null OR syncState != null`, so once everyone is synced all ~N checked-in rows are re-SELECTed each `CHECKIN_SYNC_INTERVAL_MS` and filtered out in memory. Wasteful only at large scale (e.g. ~10k concurrent check-ins). Fix needs a schema change — either a persisted `checkinNeedsSync` boolean set at every check-in/undo/soft-delete/un-delete write site and cleared on push (simplest query, but a missed write site = silent no-sync), or a derived `checkinSyncedAt` DateTime column compared against `confirmedCheckedInAt` via raw SQL (dirtiness stays derived from truth, no write-site discipline). Deferred from the 2026-07-17 code review.
